# Core Flet framework
flet

# HTTP requests for NOAA / aurora data
requests

# Faster JSON parsing (if needed for large data)
# orjson>=3.9.0

# Charts / graphs for aurora activity visualization
# matplotlib>=3.8.0
# plotly>=6.0.0

# Background scheduling / periodic fetch / auto-refresh, etc
# APScheduler>=3.12.0

# Async HTTP requests (for future scalability)
# httpx>=0.26.0